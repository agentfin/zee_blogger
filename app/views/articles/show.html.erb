<h1><%= provide(:title, @article.title) %></h1>
	<h1>
		<div class="center hero-unit"><%= @article.title %>
		</div>
	</h1>
	
	<h2><%= @article.body %></h2>
	
	<br />
	
	<p>Tags: <%= tag_links(@article.tags) %></p>
	
	<br />
	
	<%= link_to "DELETE", article_path(@article), :method => :delete, :confirm => "Really delete the article?" %><p>...</p>
	<%= link_to "Edit", edit_article_path(@article) %>
	
	<br />

	<br />		
	<%= link_to "<< Back to Articles List", articles_path %>
	<br />
	<br />
	
	<%= render :partial => 'comment_form' %>
	



<br /><h3>Comments<br /> (So far <%= @article.comments.map { |comment| comment.author_name }.uniq.count %> people give a crap about this)</h3>
	<%= render :partial => 'comment', :collection => @article.comments %>